# v0.6.0 dogfood verification

**Cycle**: v0.6.0 bug-fix cycle · **Date**: 2026-06-11
**Plugin source under test**: `~/Repositories/spec2ship/spec2ship` @ `fix/TECH-011-remove-vestigial-debate-sides` (`4edf3a6`), confirmed via a pre-run gate.
**Raw data**: `ElfGiftRush_s2s/exp65` (local-only per `feedback_test_data_split`).

## Methodology

Per `feedback_dogfood_cadence`: script bugs verified directly; the one instruction change touching runtime flow (TECH-011, debate session-setup) verified by one user-driven live run. Single focused run rather than a batch.

| Item | How verified |
|---|---|
| TECH-011 | **exp65** — `/s2s:roundtable "Canvas vs DOM" --strategy debate --participants software-architect,technical-lead`, 3 rounds, concluded |
| BUG-017 / BUG-018 / BUG-019 / BUG-020 | Direct — hermetic test harness `skills/roundtable-execution/scripts/tests/test-token-tracker.sh` (26 assertions) + static; BUG-018/019 also incidentally confirmed live in exp65 |

**False start (recorded)**: the first attempt (exp64) pointed the plugin at the stale `~/code/github.com/spec2ship/spec2ship` clone (`a4ffa3a` = v0.5.0 code), so it tested pre-v0.6.0 code (`debate_sides`, `workflowType`, 200k all present). exp65 added a pre-run gate (verify source clone has `token-tracker` v5.5.0 + 0 `debate_sides`) and re-ran against the correct code. See `project_spec2ship_clone_sprawl` memory.

## Per-item verdict

| Item | Verdict | Where | Evidence (exp65 artifacts) |
|---|---|---|---|
| **TECH-011** (vestigial debate side-assignment removed) | **PASS** | exp65 | `session.yaml` has **0** `debate_sides` fields; `agent_state.facilitator.hook_overrides` carries `participant_response_field: "debate_role"`, `policy: "facilitator_emergent"`; debate ran 3 rounds with Pro/Con sides (architect=Pro, tech-lead=Con) and concluded with 2 accepted decisions — no error from the removed `assign_debate_sides` pre-step |
| **BUG-019** (context limit adapts to model window) | **PASS** (live + unit) | exp65 + tests | `context-window.json`: `context_window_size: 1000000`; cache `startTokens=160000` (= ~16% of 1M, not of 200k — the old code would have read ~32k); no premature `SHOULD_STOP` across the session (~130k ≈ 13% of 1M) |
| **BUG-018** (vestigial write-only cache fields removed) | **PASS** (live + unit) | exp65 + tests | exp65 cache has **no** `workflowType`/`strategy`/`phase`/`participantsCount` (confirms v5.5.0 token-tracker ran); statusline RT info unaffected |
| **BUG-017** (recap survives /compact) | **PASS** (unit) | tests | Test 1 reproduces the exp61 symptom (negative deltas / 0%) and asserts the fix; not re-exercised live (compact sub-test optional, recap logic is unit-covered) |
| **BUG-020** (statusline bar window-agnostic) | **PASS** (unit/static) | tests + arithmetic | `FILLED = round(used_pct/10)`; identical to old `USED_K/20` at 200k, correct at 1M. Fallback bar not exercised live (user chains to a global statusline) |

## Notes

- **Plugin-self observations** from the exp65 run (the model narrated these; both consistent with v0.6.0): agent resume via `SendMessage`/agentId worked across all 6 facilitator/participant resumes; token-tracker scaled to the 1M Opus window with no premature pause (BUG-019 live).
- The exp65 session wrote memories into its **own throwaway worktree store** (cwd-keyed to `ElfGiftRush_s2s/exp65`), not the spec2ship store — discarded with the worktree; the authoritative spec2ship memory already records BUG-019.
- Outcome of the debate (incidental, for ElfGiftRush): DOM by default + renderer-agnostic `render()` seam, Canvas at ≥~50 sprites or per-frame VFX (DEC-001/002; OQ-001/002 deferred).

**Gate cleared** for the v0.6.0 release.
