# v0.8.0 quality-release dogfood — structural verification

**Captured**: 2026-07-12
**Source**: dogfood worktree exp50 (local-only repo, raw data NOT in this repo)
**Plugin commits under test**: develop `3900bc7` (PRs #37-#40); BUG-026 re-test @ fix branch `0ec43fe` (merged as PR #41, `c1c29a9`)
**Dogfood commits (raw)**: fixture `ba1b0c6`, main run `ca1fbd1`, BUG-026 re-test `811314c` on branch `exp50` in the dogfood repo
**Driver**: piloted local tmux Claude session (scenarios from the colocated RUNBOOK.md; user-authorized pilot run)
**Purpose**: verify the v0.8.0 instruction-layer changes (BUG-025 arc42 completeness, FEAT-012 roundtable quality, FEAT-013 plan grounding) plus the TECH-013 carry-over from v0.7.0. Scenario scripts derive from the `proposed_test` column of the Vektra validation matrix (analysis doc-set 02).

---

## Run conditions

| Variable | Value |
|----------|-------|
| Fixture | pre-init commit + seeded baseline requirements doc (6 features, 2 seeded to contradict, 2 quality targets), source files with deliberate code-vs-docs drift, pre-existing `.gitignore` |
| Sessions | init (~8m) · specs 5 rounds `--interactive` consensus-driven (~42m) · design 3 rounds debate (~48m) · plan `--all` (~21m) · 2 throwaway panel-check sessions |
| Panel | specs default 5 (incl. technical-lead, new); design default 4; panel-check runs with 2 via `--participants` |

## Per-VKT verdicts

| VKT | Backlog | Check | Verdict |
|-----|---------|-------|---------|
| VKT-073 | TECH-013 | `.s2s/local/` appended to a pre-existing `.gitignore`, prior entries intact; privacy note in init summary | **PASS** |
| VKT-061 | FEAT-012 | `--participants` override honored on specs (profile now `configurable: true`); 2-member panel actually ran | **PASS** |
| VKT-035 | FEAT-012→BUG-026 | missing-technical-role warning | **FAIL at first run** (display-only step skipped, BUG-013 class) → fixed in PR #41 with an artifact-backed carrier; re-test: session file born with the entry in `validation.warnings` → **PASS** (display line remains best-effort; skipped again in a non-interactive run) |
| VKT-004 | FEAT-012 | baseline doc parsed into 8 `BASE-*` items in `context-snapshot.input_sources`; artifacts cite BASE ids via `related_to`; deliberate premature conclude REJECTED by Step 2.9b gate 5 naming the uncovered item; final coverage 8/8 | **PASS** |
| VKT-006 | FEAT-012 | injected requirement contradicting an approved artifact → CONF raised in-round by the facilitator, panel-unanimous block, escalated to user with resolution options | **PASS** (in-round duty; 2.9b sweep untriggered because nothing contradictory survived to conclude) |
| VKT-063 | BUG-025 | specs: 32/32 artifact ids in requirements.md, summary line `Fidelity check: all 28 approved artifact IDs rendered`; design: 20/20 ARCH/COMP/INT ids, summary `all 20 artifact IDs rendered` | **PASS** |
| VKT-025 | BUG-025 | architecture.md carries ALL 12 arc42 sections + Traceability appendix; 0 placeholders needed (rich session) | **PASS** |
| VKT-026 | BUG-025 | 5 Mermaid diagrams, artifact-backed: context (flowchart LR), building blocks + deployment (flowchart TB), runtime (sequenceDiagram), quality tree (mindmap) | **PASS** |
| VKT-082 | BUG-025 | §10.1 quality-tree mindmap over 6 NFRs; §10.2 scenarios table with target/minimum/measurement + ARCH cross-refs | **PASS** |
| VKT-060/016 | FEAT-013 | codebase-analyzer invoked as an Agent (subagent_type `s2s:exploration:codebase-analyzer`) before work-item identification; plans built from source state; drift reconciliation is an explicit plan task | **PASS** |
| VKT-031 | FEAT-013 | seeded drift flagged, not overwritten (type-count, value scale, forbidden ambient APIs, singleton-vs-factory); already-matching code marked keep-as-is | **PASS** |
| VKT-032 | FEAT-013 | Test Infrastructure block (required/provided-by/false-pass guard) present in 11/11 generated plans | **PASS** |
| VKT-042 | FEAT-013 | State & Data Lifecycle table with runtime create/update/invalidate paths in 11/11 plans | **PASS** |
| VKT-043 | FEAT-013 | NFR Verification table (dataset/tool/env/pass criterion) in 11/11 plans | **PASS** |

**Tally: 13/14 PASS at first run; the single FAIL (VKT-035 display) fixed as BUG-026 and re-verified the same night. All v0.8.0 items dogfood-clear.**

## Structural invariants observed

| Invariant | Value |
|-----------|-------|
| specs artifacts | 6+3 REQ, 4 BR, 6 NFR, 9 EX, 3+1 OQ, 1 CONF (resolved) — 32 ids total, 28 approved |
| design artifacts | 11 ARCH → 11 ADR files, 7 COMP, 2 INT — 20 ids |
| ADR format | `Source: ARCH-*` present; participants list = session panel (not hardcoded trio) |
| plans | 11 files, all with the 3 structured template blocks filled |
| conclude gates | composite rejection fired verbatim: agenda gate (<50% non-critical) + baseline gate (named uncovered item) in one `validation_override` |
| token tracking | TECH-009 estimate/actual persisted per round; statusline dynamic 1M window live (BUG-019 behavior) |

## Findings carried forward

1. **BUG-026** (fixed in-cycle): display-only steps remain unreliable even for "always shown" blocks; warnings must be artifact-backed. Display carrier tracked as best-effort.
2. **Polish (unfiled)**: template HTML comments (`<!-- ... -->`) are copied verbatim into generated plan files; harmless noise, candidate one-line instruction fix next cycle.
3. VKT-006's command-side sweep (2.9b gate 6) was not exercised — the facilitator caught both the seeded and the injected contradictions in-round. The gate remains verified only statically.
